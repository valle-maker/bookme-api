spring.application.name=bookme-api
spring.profiles.active=dev
server.port=8080

# ─── Resend (HTTP API — no SMTP) ─────────────────────────────────────────────
resend.api-key=${RESEND_API_KEY:}

# Frontend URL para enlaces de email
app.frontend-url=${APP_FRONTEND_URL:http://localhost:3000}
